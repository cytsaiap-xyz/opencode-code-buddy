---
description: "🤖 Ask AI using OpenCode's current LLM"
---

Use the `buddy_ask_ai` tool to ask a question using OpenCode's current LLM.

Question or topic: $ARGUMENTS

This will use whatever LLM is currently configured in OpenCode, so no additional API configuration is needed.
