---
description: "📝 Add a memory entry to Code Buddy"
---

Use the `buddy_add_memory` tool to add a new memory entry.

The user wants to record: $ARGUMENTS

Please determine an appropriate:
- Title (concise summary)
- Content (detailed description)
- Type (one of: decision, pattern, bugfix, lesson, feature, note)

Then call `buddy_add_memory` with these values.
